---
layout: course
title: "Readme"
permalink: /1_Foundations/python-math-foundations-10weeks/code/week08/README.html
---

# Tuần 8 — Code minh họa

## 01_vector_matrix.py

**Chức năng:** Tính tích vô hướng, tổ hợp tuyến tính, nhân ma trận và bắt lỗi kích thước.

**Chạy:** `python code/week08/01_vector_matrix.py` (cần `pip install numpy`)

**Kết quả mong đợi:** Kết quả các phép toán và thông báo lỗi kích thước ma trận.

```python
{% include_relative 01_vector_matrix.py %}
```

## 02_softmax_entropy.py

**Chức năng:** Cài đặt softmax ổn định số, entropy và cross-entropy.

**Chạy:** `python code/week08/02_softmax_entropy.py` (cần `pip install numpy`)

**Kết quả mong đợi:** Phân phối softmax có tổng = 1, entropy đều vs chắc chắn, cross-entropy.

```python
{% include_relative 02_softmax_entropy.py %}
```
