# Tuần 8: Huấn Luyện Mô Hình — Review Questions

1. Giải thích cấu trúc đầu vào `X` và nhãn đáp án `Y` trong hàm `get_batch`. Tại sao `Y` lại là `X` dịch phải 1 ký tự?
2. Hàm tối ưu hóa AdamW và thuật toán lan truyền ngược (Backpropagation) phối hợp với nhau như thế nào để cập nhật trọng số?
3. Điều gì xảy ra nếu ta đặt Learning Rate quá lớn (ví dụ `1.0`) hoặc quá nhỏ (ví dụ `1e-8`)? Làm thế nào để chọn được một Learning Rate phù hợp?
